{
  "name": "fakeapi",
  "version": "1.0.0",
  "description": "api fake",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch db.json"

  },
  "author": "alex",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
